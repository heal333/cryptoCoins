const LoadingDiv = () => {
    return (
        <div
            className={`flex items-center justify-center h-full text-white text-5xl bg-gradient-to-b from-gray-900 to-gray-500`}
        >
            loading ...
        </div>
    );
};

export default LoadingDiv;
